# Build outputs
/build
/app/build

# Local properties (contains SDK path - machine specific)
local.properties

# IDE files
.idea/
*.iml
.gradle/

# API keys / production secrets (never commit these)
# Note: watercan-release.jks is a DEBUG keystore — safe to commit for consistent signing
# DO NOT add app/watercan-release.jks here
